(function() {
    const canvas = document.getElementById('canvas-simple');
    const context = canvas.getContext('2d'); // Can be 3d
    
    let x = 175;
    let y = 175;
    
    context.fillStyle = '#fff';
    context.fillRect(x, y, 40, 40);
}());





